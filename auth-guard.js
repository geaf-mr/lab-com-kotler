const ACCESS_HASH="cbc70bdc78b4defe27819049c4060946f16a74c03909047f84f4304696b145f4";
const SESSION_KEY="kotler-teacher-auth";
async function digest(value){const bytes=new TextEncoder().encode(value);const hash=await crypto.subtle.digest("SHA-256",bytes);return [...new Uint8Array(hash)].map(b=>b.toString(16).padStart(2,"0")).join("")}
function modal(){const wrap=document.createElement("div");wrap.className="auth-overlay";wrap.innerHTML=`<form class="auth-card"><div class="auth-icon">MK</div><span class="tag">Acceso restringido</span><h2>Ingreso docente</h2><p class="context">Identificate para acceder a la consola y crear salas.</p><label><span>Correo docente</span><input id="teacher-email" type="email" autocomplete="username" required placeholder="docente@institucion.edu"></label><label><span>Clave de acceso</span><input id="teacher-password" type="password" autocomplete="current-password" required placeholder="••••••••••••"></label><div class="auth-error" hidden>La clave de acceso no es correcta.</div><div class="auth-actions"><button type="button" class="btn secondary" data-auth-cancel>Cancelar</button><button class="btn">Ingresar</button></div><small>Acceso provisional. La autenticación institucional se incorporará en la próxima etapa.</small></form>`;document.body.appendChild(wrap);wrap.querySelector("[data-auth-cancel]").onclick=()=>wrap.remove();wrap.addEventListener("click",e=>{if(e.target===wrap)wrap.remove()});wrap.querySelector("form").onsubmit=async e=>{e.preventDefault();const password=wrap.querySelector("#teacher-password").value;if(await digest(password)!==ACCESS_HASH){wrap.querySelector(".auth-error").hidden=false;return}sessionStorage.setItem(SESSION_KEY,JSON.stringify({email:wrap.querySelector("#teacher-email").value,at:Date.now()}));wrap.remove();document.querySelector('[data-action="teacher"]')?.click()};setTimeout(()=>wrap.querySelector("#teacher-email").focus(),0)}
document.addEventListener("click",e=>{const trigger=e.target.closest('[data-action="teacher"]');if(!trigger||sessionStorage.getItem(SESSION_KEY))return;e.preventDefault();e.stopImmediatePropagation();modal()},true);
