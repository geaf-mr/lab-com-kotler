import{loginTeacher,loadStudentRoom,firebaseError}from"./firebase-bridge.js";
let studentReady=false;
function modal(){const wrap=document.createElement("div");wrap.className="auth-overlay";wrap.innerHTML=`<form class="auth-card"><div class="auth-icon">MK</div><span class="tag">Acceso restringido</span><h2>Ingreso docente</h2><p class="context">Usá la misma cuenta docente de Taller TOOI.</p><label><span>Correo docente</span><input id="teacher-email" type="email" autocomplete="username" required placeholder="docente@institucion.edu"></label><label><span>Contraseña</span><input id="teacher-password" type="password" autocomplete="current-password" required placeholder="••••••••••••"></label><div class="auth-error" hidden></div><div class="auth-actions"><button type="button" class="btn secondary" data-auth-cancel>Cancelar</button><button class="btn">Ingresar</button></div></form>`;document.body.appendChild(wrap);wrap.querySelector("[data-auth-cancel]").onclick=()=>wrap.remove();wrap.querySelector("form").onsubmit=async e=>{e.preventDefault();const error=wrap.querySelector(".auth-error"),submit=wrap.querySelector("button:not([type])");error.hidden=true;submit.disabled=true;try{await loginTeacher(wrap.querySelector("#teacher-email").value,wrap.querySelector("#teacher-password").value);sessionStorage.setItem("kotler-teacher-auth","1");wrap.remove();document.querySelector('[data-action="teacher"]')?.click()}catch(x){error.textContent=firebaseError(x);error.hidden=false;submit.disabled=false}}}
document.addEventListener("click",async e=>{const teacher=e.target.closest('[data-action="teacher"]');if(teacher&&!sessionStorage.getItem("kotler-teacher-auth")){e.preventDefault();e.stopImmediatePropagation();modal();return}const enter=e.target.closest('[data-action="enter"]'),code=document.querySelector("#code")?.value.trim().toUpperCase();if(!enter||!code||studentReady)return;e.preventDefault();e.stopImmediatePropagation();const error=document.querySelector("#error");enter.disabled=true;try{await loadStudentRoom(code);studentReady=true;enter.disabled=false;enter.click();studentReady=false}catch(x){error.textContent=firebaseError(x);error.hidden=false;enter.disabled=false}},true);
